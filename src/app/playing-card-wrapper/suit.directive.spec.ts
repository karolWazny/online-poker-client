import { SuitDirective } from './suit.directive';

describe('SuitDirective', () => {
  it('should create an instance', () => {
    const directive = new SuitDirective();
    expect(directive).toBeTruthy();
  });
});
