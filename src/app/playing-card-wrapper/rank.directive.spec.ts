import { RankDirective } from './rank.directive';

describe('RankDirective', () => {
  it('should create an instance', () => {
    const directive = new RankDirective();
    expect(directive).toBeTruthy();
  });
});
